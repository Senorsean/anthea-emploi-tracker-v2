[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — React Router (sinon 404 au refresh des routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
